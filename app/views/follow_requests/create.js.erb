console.log("Followed")

var revertFollowForm = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient) %>" )

$("#follow_request_form").replaceWith(revertFollowForm)
