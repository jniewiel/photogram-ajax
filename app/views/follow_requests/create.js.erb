console.log("Followed")

var oldFollowForm = $(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient) %>_follow_unfollow")

var newFollowForm = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient) %>" )

oldFollowForm.replaceWith(newFollowForm)
