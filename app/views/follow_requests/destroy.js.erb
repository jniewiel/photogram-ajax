console.log("Unfollowed")

var oldUnfollowForm = $(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient) %>_follow_unfollow")

var newUnfollowForm = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient) %>")

oldUnfollowForm.replaceWith(newUnfollowForm)
